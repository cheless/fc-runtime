import { ConfigManager } from './ConfigManager';
import { ConsumerManager } from '../object-proxying/ConsumerManager';
export declare class ConfigClient {
    static create(consumerManager: ConsumerManager): Promise<ConfigClient>;
    configManagerProxy: ConfigManager;
    constructor(configManagerProxy: ConfigManager);
    subscribe(topic: any, cb: any): Promise<void>;
    unsubscribe(topic: any, cb: any): Promise<void>;
    getConfig(topic?: string): Promise<any>;
}
//# sourceMappingURL=ConfigClient.d.ts.map