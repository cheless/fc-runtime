import { IComponentDeclaration } from './types';
export interface ISortedItem {
    name: string;
    weight: number;
}
export declare class ComponentWeightCalculator {
    private components;
    constructor(components: Map<string, IComponentDeclaration>);
    getWeight(name: any, chain?: string[]): number;
    getSortedComponentNames(order: 'asc' | 'desc'): ISortedItem[];
}
//# sourceMappingURL=ComponentWeightCalculator.d.ts.map